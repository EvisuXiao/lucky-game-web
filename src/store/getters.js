const getters = {
  username: state => state.common.username,
  isFront: state => state.common.isFront,
  loading: state => state.common.loading,
  rowTmp: state => state.common.rowTmp
}
export default getters
